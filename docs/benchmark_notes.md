# Benchmark Notes

No benchmark has been run because this workspace has no Python runtime, local YOLO weights, or sample video. Run `python scripts/benchmark_pipeline.py <local-video>` after provisioning those inputs, with `nvidia-smi dmon` in a separate terminal, then record FPS and the limiting stage here.
